#!/bin/bash

echo "Updating DarkEye_dev"

git pull
git checkout main
git pull
git checkout release
git merge main --no-edit
git commit -a -m "Update from server"
git push
if [ "$1" == "all" ]; then
    echo "\nUpdating all packages by running swift package update"
    swift package update
fi

